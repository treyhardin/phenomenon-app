---
---
<header>
  <a class="logo utility" href="/">The Phenomenon</a>
  <div class="search-field">
    <input type="search" id="search" name="q" placeholder="Search location, keyword, etc."/>
    <label hidden for="search">Search location, keyword, etc.</label>
    <button>⌘ &#x2b; K</button>
  </div>
  <button id="submit-report">Submit Report</button>
</header>

<style>
  header {
    padding-inline: var(--page-margin);
    padding-block: var(--space-sm);
    display: flex;
    flex-direction: row;
    justify-content: stretch;
    align-items: center;
    gap: var(--space-sm);
    /* position: sticky;
    top: 0; */

    .logo {
      flex: 0 0 auto;
      word-break: keep-all;
    }

    .search-field {
      flex: 0 1 100%;
      position: relative;

      #search {
        width: 100%;
      }

      button {
        position: absolute;
        right: var(--space-2xs);
        top: 50%;
        translate: 0 -50%;
        padding: 0.2em 0.6em;
        background-color: var(--color-background-secondary);
        color: var(--color-neutral);
        border: 1px solid var(--color-neutral);
      }
    }

    #submit-report {
      flex: 0 0 auto;
      word-break: keep-all;
    }
  }


</style>